From fe3b756fc04fd59df7ea6576a286b938596e97fd Mon Sep 17 00:00:00 2001
From: Alistair Leslie-Hughes <leslie_alistair@hotmail.com>
Date: Fri, 1 Jul 2016 14:09:29 +1000
Subject: [PATCH] winspool.drv: Assign an empty pointer if Printer not
 specified

Fixes: https://bugs.winehq.org/show_bug.cgi?id=33502

Signed-off-by: Alistair Leslie-Hughes <leslie_alistair@hotmail.com>
---
 dlls/winspool.drv/info.c | 12 ++++++++++++
 1 file changed, 12 insertions(+)

diff --git a/dlls/winspool.drv/info.c b/dlls/winspool.drv/info.c
index 3ba94cd..b8edf62 100644
--- a/dlls/winspool.drv/info.c
+++ b/dlls/winspool.drv/info.c
@@ -4311,6 +4311,18 @@ static BOOL WINSPOOL_GetPrinter_5(HKEY hkeyPrinter, PRINTER_INFO_5W *pi5,
 	    space = FALSE;
 	*pcbNeeded += size;
     }
+    else
+    {
+        /* Assign an empty port */
+        if(space && size <= left) {
+	    pi5->pPortName = (LPWSTR)ptr;
+            *pi5->pPortName = '\0';
+	    ptr += sizeof(WCHAR);
+	    left -= sizeof(WCHAR);
+	} else
+	    space = FALSE;
+	*pcbNeeded += sizeof(WCHAR);
+    }
     if(pi5) {
         pi5->Attributes = get_dword_from_reg( hkeyPrinter, AttributesW );
         pi5->DeviceNotSelectedTimeout = get_dword_from_reg( hkeyPrinter, dnsTimeoutW );
-- 
1.9.1

